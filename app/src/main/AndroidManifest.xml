<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/hiking"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/hiking"
        android:supportsRtl="true"
        android:theme="@style/Theme.MHike">

        <activity
            android:name=".AddObservationActivity"
            android:exported="false"
            android:parentActivityName=".ObservationActivity" />

        <activity
            android:name=".ObservationActivity"
            android:exported="false"
            android:parentActivityName=".HikeDetailActivity" />

        <activity
            android:name=".HikeDetailActivity"
            android:exported="false"
            android:parentActivityName=".HomePage" />

        <activity
            android:name=".AddHikeActivity"
            android:exported="false"
            android:parentActivityName=".HomePage" />

        <activity
            android:name=".HomePage"
            android:exported="false" />

        <activity
            android:name=".WelcomePage"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".SignUp"
            android:exported="false" />
        <activity
            android:name=".SignIn"
            android:exported="false" />

    </application>

</manifest>